import { describe, expect, it } from "vitest";

describe("二叉树的最近公共祖先", () => {});

function testCase(fn: unknown) {
  it.each([
    // test cases
  ])("示例%#", () => {
    expect(true).toBe(true);
  });
}
